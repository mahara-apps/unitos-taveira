// Placeholder — the Supabase client already lives at
// `@/integrations/supabase/client`. This module re-exports it so feature code
// can depend on the `lib/supabase` boundary described in the architecture.
export { supabase } from "@/integrations/supabase/client";
